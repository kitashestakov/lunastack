---
name: lunastack-schedule-deprecated
description: |
  ВНИМАНИЕ: этот скилл переименован в /meet чтобы не пересекаться со встроенным
  /schedule в Claude Code (который создаёт scheduled remote agents).
  НЕ запускай этот скилл — используй /meet.
---

# DEPRECATED: используй /meet вместо /schedule

Этот файл оставлен только для истории. Реальный скилл назначения звонков находится в `.claude/skills/meet/SKILL.md`.

Если рекрутер набрал `/schedule` — ему ответит встроенный Claude Code скилл (про scheduled remote agents). Чтобы поставить звонок, нужно набрать `/meet`.
